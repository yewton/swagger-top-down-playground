'use strict';

exports.findstore = function(tags, limit) {

  var examples = {};
  
  examples['application/json'] = [ "" ];
  

  
  if(Object.keys(examples).length > 0)
    return examples[Object.keys(examples)[0]];
  
}
